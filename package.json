{
  "name": "context-compiler",
  "version": "0.1.0",
  "description": "Task-aware, token-budgeted file-to-markdown compiler for AI agents (MCP server + web demo)",
  "type": "module",
  "bin": {
    "context-compiler": "dist/server.js"
  },
  "scripts": {
    "build": "tsc && tsc -p tsconfig.client.json",
    "test": "npm run build && NODE_ENV=test node dist/tests/test.js",
    "mcp": "node dist/server.js",
    "web": "node dist/web.js",
    "lint": "eslint .",
    "format": "prettier --write \"src/**/*.ts\" \"*.{js,json}\"",
    "format:check": "prettier --check \"src/**/*.ts\" \"*.{js,json}\""
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@modelcontextprotocol/sdk": "^1.11.0",
    "express": "^4.21.0",
    "js-tiktoken": "^1.0.15",
    "multer": "^2.2.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/multer": "^2.0.0",
    "@types/node": "^22.10.0",
    "eslint": "^9.39.5",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.9.5",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.64.0"
  }
}
