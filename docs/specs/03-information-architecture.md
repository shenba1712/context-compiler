# Information architecture

**Surfaces:** MCP tools · web demo (`apps/web` Next UI + Nest API)

## Web

Marketing landing and a focused demo workspace with nested steps (compile → results → prove → agent), plus `/mcp` for install copy. See [05-ui-ux-flow.md](./05-ui-ux-flow.md).

## MCP

Exactly two tools: `compile_context`, `expand_section`. Paths confined to `CC_ROOT`.
